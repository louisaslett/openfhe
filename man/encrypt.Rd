% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/encrypt.R
\name{encrypt}
\alias{encrypt}
\alias{decrypt}
\alias{encrypt,ANY,FHEKeys-method}
\alias{encrypt,ANY,FHEPublicKey-method}
\alias{decrypt,Ciphertext,FHEKeys-method}
\alias{decrypt,Ciphertext,FHESecretKey-method}
\title{Encrypt and decrypt vectors}
\usage{
encrypt(x, keys, ...)

decrypt(ct, keys, ...)

\S4method{encrypt}{ANY,FHEKeys}(x, keys, ...)

\S4method{encrypt}{ANY,FHEPublicKey}(x, keys, ...)

\S4method{decrypt}{Ciphertext,FHEKeys}(ct, keys, ...)

\S4method{decrypt}{Ciphertext,FHESecretKey}(ct, keys, ...)
}
\arguments{
\item{x}{The vector to encrypt: numeric for CKKS; whole-valued numeric,
integer or logical for any scheme.  Any length: vectors longer than
\code{\link[=slot_count]{slot_count()}} are split across several underlying ciphertexts
automatically.}

\item{keys}{For \code{encrypt()} an \link[=FHEKeys-class]{FHEKeys} or \code{FHEPublicKey};
for \code{decrypt()} an \link[=FHEKeys-class]{FHEKeys} or \code{FHESecretKey}.}

\item{...}{Unused; reserved for future methods.}

\item{ct}{A \link[=Ciphertext-class]{Ciphertext} to decrypt.}
}
\value{
\code{encrypt()} returns a \link[=Ciphertext-class]{Ciphertext}.  \code{decrypt()} returns
a numeric vector (CKKS; approximate) or an integer vector (BFV/BGV;
exact, or numeric if any value exceeds R's integer range), of the
same length as the originally encrypted vector.
}
\description{
\code{encrypt()} turns an R vector into a \link[=Ciphertext-class]{Ciphertext}: elements are
packed into the SIMD slots of an OpenFHE ciphertext (\code{\link[=slot_count]{slot_count()}}
per ciphertext), and longer vectors transparently spill across as many
underlying ciphertexts as needed -- vector length is unlimited (until
memory runs out).  \code{decrypt()} recovers the plaintext vector, at its
original length.  Both are data-first, so they read naturally in a
pipe: \code{x |> encrypt(keys) |> decrypt(keys)}.  See \code{\link[=chunks]{chunks()}} to look
at the underlying ciphertexts individually.
}
\section{Scheme-aware encoding}{

\itemize{
\item \strong{CKKS} encrypts real numbers \emph{approximately}: \code{decrypt()} returns
values close to, but not exactly equal to, the inputs (typically to
many decimal places; see \code{scale_bits} in \code{\link[=fhe_context]{fhe_context()}}).
\item \strong{BFV/BGV} encrypt integers \emph{exactly}, modulo the context's
\code{plaintext_modulus}.  Values must be whole numbers with magnitude at
most \code{(plaintext_modulus - 1) / 2}; anything else is an error --
use CKKS for real-valued data.
\item \strong{Logical} vectors are encrypted as 0/1 (with a message);
complex and character vectors cannot be encrypted.
}

Encryption needs only the public key: \code{encrypt(x, keys)} and
\code{encrypt(x, keys@public_key)} are equivalent, so a data owner can
encrypt against a public key received from elsewhere.  Decryption
requires the secret key.
}

\examples{
ctx <- fhe_context("CKKS", mult_depth = 2, security = NA, ring_dim = 1024)
keys <- keygen(ctx)
ct <- encrypt(c(1.5, -2.25, 3), keys)
ct
decrypt(ct, keys)

# exact integer arithmetic under BFV
ctx <- fhe_context("BFV", mult_depth = 2, security = NA, ring_dim = 1024)
keys <- keygen(ctx)
decrypt(encrypt(c(-3L, 0L, 42L), keys), keys)
}
\seealso{
\code{\link[=keygen]{keygen()}}, \code{\link[=fhe_context]{fhe_context()}}
}
