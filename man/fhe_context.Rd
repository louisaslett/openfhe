% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/context.R
\name{fhe_context}
\alias{fhe_context}
\title{Create a homomorphic encryption context}
\usage{
fhe_context(
  scheme = c("CKKS", "BFV", "BGV"),
  mult_depth = 10,
  security = 128,
  ring_dim = NULL,
  batch_size = NULL,
  scale_bits = 50,
  first_mod_bits = 60,
  scaling = c("auto", "auto-ext", "fixed"),
  bootstrap = FALSE,
  plaintext_modulus = 65537
)
}
\arguments{
\item{scheme}{\code{"CKKS"} (approximate reals, the default), \code{"BFV"} or
\code{"BGV"} (exact modular integers).}

\item{mult_depth}{Multiplicative depth budget: the maximum number of
sequential ciphertext multiplications that can be applied before
decryption fails (or bootstrapping is needed).  Larger budgets cost
memory and time.}

\item{security}{Target security level in bits: \code{128}, \code{192}, \code{256}, or
\code{NA} for unvalidated toy parameters (see Security section).}

\item{ring_dim}{Ring dimension, a power of two.  Leave \code{NULL} (default)
to let OpenFHE choose the smallest secure dimension; must be supplied
when \code{security = NA}.}

\item{batch_size}{Number of SIMD slots to encode per ciphertext.  Leave
\code{NULL} for the maximum (\code{ring_dim/2} for CKKS, \code{ring_dim} for
BFV/BGV).}

\item{scale_bits}{CKKS only: bits of precision of the encoding scaling
factor (default 50).  More bits, more precision, bigger parameters.}

\item{first_mod_bits}{CKKS only: bits of the first modulus (default 60);
must exceed \code{scale_bits}.}

\item{scaling}{CKKS only: rescaling management.  \code{"auto"} (default,
FLEXIBLEAUTO) and \code{"auto-ext"} (FLEXIBLEAUTOEXT) rescale automatically;
\code{"fixed"} (FIXEDMANUAL) is for experts who rescale by hand.}

\item{bootstrap}{CKKS only: set \code{TRUE} if you intend to bootstrap
ciphertexts created under this context (enables the FHE feature set;
bootstrapping keys are generated at key-generation time).}

\item{plaintext_modulus}{BFV/BGV only: the integer plaintext modulus
\eqn{t}; all arithmetic is exact modulo \eqn{t}.  The default 65537 is
prime and supports SIMD packing for every \code{ring_dim} up to 32768.}
}
\value{
An \link[=FHEContext-class]{FHEContext} object.
}
\description{
A context fixes the encryption scheme and all of its parameters; keys,
plaintext encodings and ciphertexts all live relative to one context.
Sensible defaults are chosen so that \code{fhe_context("CKKS")} just works;
the parameters below are the ones users typically need to touch.
}
\section{Choosing a scheme}{

\itemize{
\item \code{"CKKS"} computes on \strong{real numbers, approximately} -- the workhorse
for statistics and machine learning.
\item \code{"BFV"} and \code{"BGV"} compute on \strong{integers, exactly}, modulo
\code{plaintext_modulus}.
}
}

\section{Security}{

\code{security = 128} (the default), \code{192} or \code{256} selects the corresponding
HomomorphicEncryption.org standard parameter sets; OpenFHE then chooses a
ring dimension automatically (unless \code{ring_dim} is forced).
\code{security = NA} disables the standard entirely and is \strong{only for toy
examples and tests}: it allows tiny, fast, INSECURE parameters and then
requires \code{ring_dim} to be given explicitly.  Never use \code{security = NA}
with real data.
}

\examples{
# Toy CKKS context: tiny, fast and INSECURE -- for examples only
ctx <- fhe_context("CKKS", mult_depth = 2, security = NA, ring_dim = 512)
ctx

# Toy BFV context for exact integer arithmetic
fhe_context("BFV", mult_depth = 2, security = NA, ring_dim = 512)
}
\seealso{
\code{\link[=scheme]{scheme()}}, \code{\link[=ring_dim]{ring_dim()}}, \code{\link[=slot_count]{slot_count()}}, \code{\link[=mult_depth]{mult_depth()}}
}
