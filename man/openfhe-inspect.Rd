% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{openfhe-inspect}
\alias{openfhe-inspect}
\alias{chunks}
\alias{chunks,Ciphertext-method}
\alias{as.character,Ciphertext-method}
\alias{as.character,FHEPublicKey-method}
\alias{as.character,FHESecretKey-method}
\alias{as.character,FHEKeys-method}
\title{Look under the hood of encrypted objects}
\usage{
chunks(x)

\S4method{chunks}{Ciphertext}(x)

\S4method{as.character}{Ciphertext}(x, ...)

\S4method{as.character}{FHEPublicKey}(x, ...)

\S4method{as.character}{FHESecretKey}(x, ...)

\S4method{as.character}{FHEKeys}(x, ...)
}
\arguments{
\item{x}{A \link[=Ciphertext-class]{Ciphertext} for \code{chunks()}; a \code{Ciphertext},
\link[=FHEKeys-class]{FHEKeys}, \code{FHEPublicKey} or \code{FHESecretKey} for
\code{as.character()}.}

\item{...}{Unused; for consistency with the base generic.}
}
\value{
\code{chunks()} returns a list of \link[=Ciphertext-class]{Ciphertext} objects.
\code{as.character()} returns a character vector: one element per
underlying ciphertext for a \code{Ciphertext}, one element for a single
key, and a named length-2 vector (\code{public_key}, \code{secret_key}) for an
\link[=FHEKeys-class]{FHEKeys}.
}
\description{
Nothing about an encrypted object is hidden from its owner: these
functions expose exactly what keys and ciphertexts are made of.
}
\details{
\code{chunks()} returns the underlying OpenFHE ciphertexts of a
\link[=Ciphertext-class]{Ciphertext} as a list of single-ciphertext \code{Ciphertext} objects.
A vector of up to \code{\link[=slot_count]{slot_count()}} elements is held in one ciphertext, so
\code{chunks()} returns a list of length one; longer vectors spill across
several (all fully packed except possibly the last), split in element
order.  Each chunk is a normal \code{Ciphertext} and can be decrypted (or,
in later phases, computed with) on its own.

\code{as.character()} renders the raw cryptographic content -- the ring
polynomials in OpenFHE's internal RNS (residue number system)
representation, one big-integer coefficient per slot per residue tower.
It is exact but \emph{large} (easily megabytes for realistic parameters), so
nothing prints it by default: \code{show} methods print compact summaries,
and viewing the raw content is an explicit act.  Display with
\code{cat(as.character(x))}.
}
\examples{
ctx <- fhe_context("BFV", mult_depth = 2, security = NA, ring_dim = 1024)
keys <- keygen(ctx)
ct <- encrypt(1:5, keys)

chunks(ct)  # short vector: a single underlying ciphertext

# the raw polynomials (truncated here; cat() the full string to see all)
substr(as.character(ct)[1], 1, 60)
}
\seealso{
\code{\link[=encrypt]{encrypt()}}, \code{\link[=keygen]{keygen()}}, \code{\link[=slot_count]{slot_count()}}
}
