% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{keygen}
\alias{keygen}
\alias{rotation_keys}
\title{Generate encryption keys for a context}
\usage{
keygen(ctx, rotations = NULL, sum = TRUE)

rotation_keys(keys, indices)
}
\arguments{
\item{ctx}{An \link[=FHEContext-class]{FHEContext} created by \code{\link[=fhe_context]{fhe_context()}}.}

\item{rotations}{Rotation indices to generate keys for: an integer
vector of non-zero shifts (positive = left, negative = right), or
\code{"power2"} for \eqn{\pm 1, \pm 2, \pm 4, \ldots} up to half the slot
count -- enough to compose any rotation, but note this generates
\eqn{2 \log_2(\mathrm{slots})} keys, which takes time and memory at
realistic parameter sizes.  \code{NULL} (default) generates none; they can
be added later with \code{rotation_keys()}.}

\item{sum}{Generate the sum (automorphism) keys needed by \code{sum()},
\code{mean()} and inner products?  Default \code{TRUE}.}

\item{keys}{An \link[=FHEKeys-class]{FHEKeys} object returned by \code{keygen()}.}

\item{indices}{As \code{rotations} above.}
}
\value{
\code{keygen()} returns an \link[=FHEKeys-class]{FHEKeys} object.
\code{rotation_keys()} returns \code{keys} invisibly, after attaching the new
rotation keys to the context.
}
\description{
\code{keygen()} generates a public/secret key pair for an \link[=FHEContext-class]{FHEContext},
together with the evaluation keys homomorphic computation needs:
}
\details{
\itemize{
\item a \strong{relinearisation key} is always generated -- every ciphertext
multiplication needs one, and generating it up front avoids confusing
failures later;
\item \strong{sum keys} (\code{sum = TRUE}, the default) enable \code{sum()}, \code{mean()} and
inner products -- core operations for statistical work; opt out with
\code{sum = FALSE} if you will never reduce across slots;
\item \strong{rotation keys} are only generated on request (see \code{rotations}),
because each index costs key material.
}

Evaluation keys are attached to the context (as OpenFHE does natively),
so they are available to every ciphertext under that context; the
returned \link[=FHEKeys-class]{FHEKeys} object holds the key pair itself.
\code{rotation_keys()} adds rotation keys for further indices to an existing
key set at any later point.
}
\examples{
ctx <- fhe_context("CKKS", mult_depth = 2, security = NA, ring_dim = 1024)
keys <- keygen(ctx)
keys

# rotation keys for specific shifts, added after the fact
rotation_keys(keys, c(1, -1))
}
\seealso{
\code{\link[=encrypt]{encrypt()}}, \code{\link[=decrypt]{decrypt()}}, \code{\link[=fhe_context]{fhe_context()}}
}
